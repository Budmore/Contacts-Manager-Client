/**
 * Demo module. Manage contacts with mocked data (CRUD)
 */
angular
	.module('demoModule', [])

	// .run(['$rootScope', function($rootScope) {
	// 	'use strict';

	// 	$rootScope.isDemoMode = false;

	// }]);
